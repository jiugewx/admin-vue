
/*view层需要直接使用的组件,请在以下注册*/
// import baseBreadcrumb from './breadcrumb/breadcrumb.vue';

const base = {
    // htBreadcrumb,

    install(Vue) {
        // Vue.component('base-breadcrumb', baseBreadcrumb);
    }
};

module.exports = base;

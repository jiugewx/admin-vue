
/*view层需要直接使用的组件,请在以下注册*/
// import htBreadcrumb from './breadcrumb/breadcrumb.vue';

const ht = {
    // htBreadcrumb,

    install(Vue) {
        // Vue.component('ht-breadcrumb', htBreadcrumb);
    }
};

module.exports = ht;
